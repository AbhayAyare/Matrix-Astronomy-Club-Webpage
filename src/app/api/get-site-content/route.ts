// This file is intentionally left blank or can be deleted.
// API Route Handlers are not compatible with `output: 'export'` in Next.js.
// Site content is fetched server-side in `src/app/page.tsx` for the main page,
// and client-side components like `SiteContentLoader` fetch directly from Firestore
// if they need dynamic content updates.
